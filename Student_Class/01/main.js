// the useState hook

// import { useState } from "react";
//  const [count, setCount] = useState(0);

// so count is the variable
// so setCount is the function

// to change the value of count we use setCount

// just like this normal function in the nor

// this is how useState work in normal javascript
function setCount(count) {
  console.log(count);
}

setCount(0);

setCount("count");

console.log(Math.floor(Math.random() * 9999)); //
